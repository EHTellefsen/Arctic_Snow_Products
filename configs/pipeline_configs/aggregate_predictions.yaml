# Configuration for aggregating predicted snow depth data into monthly means

# Input configuration for the directory containing prediction files
predictions_directory: "data/processed/predictions/"

# Output configuration for the aggregated monthly mean files
output_directory: "data/processed/predictions_aggregated/"

# Time period for which to aggregate predictions
time_period:
  start: "2012-11-01"
  end: "2025-05-01"